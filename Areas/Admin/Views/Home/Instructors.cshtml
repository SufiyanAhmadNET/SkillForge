@using SkillForge.Areas.Admin.Models
@model IEnumerable<InstructorListVM>
@{
    ViewData["Title"] = "Instructors";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<main class="p-4 flex-grow-1 admin-bg">

    <div class="d-flex align-items-center justify-content-between mb-4">
        <div class="page-title-bar">
            <h5 class="fw-bold mb-0">Instructor List</h5>
            <p class="text-muted mb-0" style="font-size:14px;">All approved instructors on SkillForge.</p>
        </div>
        <a href="#" class="btn btn-sm btn-outline-success">
            <i class="bi bi-download me-1"></i>Export CSV
        </a>
    </div>

    <!-- Filter row -->
    <div class="d-flex align-items-center gap-3 mb-3 flex-wrap">
        <div class="input-group input-group-sm" style="max-width:260px;">
            <span class="input-group-text bg-white"><i class="bi bi-search text-muted"></i></span>
            <input type="text" class="form-control" id="instructorSearch" placeholder="Search by name…">
        </div>
        <select class="form-select form-select-sm" id="statusFilter" style="max-width:150px;">
            <option value="all">All Status</option>
            <option value="Approved">Approved</option>
            <option value="Pending">Pending</option>
            <option value="Rejected">Rejected</option>
            <option value="NotApplied">Not Applied</option>
        </select>
        <small class="text-muted ms-auto">Showing @Model.Count() instructors</small>
    </div>

    <!-- INSTRUCTORS TABLE -->
    <div class="card border-0 admin-card">
        <div class="card-body p-0">
            <div class="table-responsive">
                <table class="table table-hover table-sm align-middle mb-0" style="font-size: 14px;">
                    <thead class="table-light text-muted" style="font-size: 12px; text-transform: uppercase; letter-spacing: 0.03rem;">
                        <tr>
                            <th class="ps-3">#</th>
                            <th>Instructor</th>
                            <th>Email</th>
                            <th>Courses</th>
                            <th>Total Students</th>
                            <th>Joined</th>
                            <th>Status</th>
                            <th class="pe-3">Actions</th>
                        </tr>
                    </thead>
                    <tbody id="instructorTableBody">
                        @{
                            int index = 1;
                        }
                        @foreach (var ins in Model)
                        {
                            <tr data-status="@ins.Status" data-name="@ins.Name.ToLower()">
                                <td class="ps-3 text-muted">@index++</td>
                                <td>
                                    <div class="d-flex align-items-center gap-2">
                                        <div class="tbl-avatar bg-primary bg-opacity-10 text-primary fw-bold" style="width: 28px; height: 28px; font-size: 11px;">
                                            @ins.Name.Substring(0, 1).ToUpper()
                                        </div>
                                        <span class="fw-medium text-dark">@ins.Name</span>
                                    </div>
                                </td>
                                <td class="text-muted small">@ins.Email</td>
                                <td class="fw-medium">@ins.TotalCourses</td>
                                <td class="text-muted">@ins.TotalStudents.ToString("N0")</td>
                                <td class="text-muted small">@ins.JoinedDate.ToString("MMM dd, yyyy")</td>
                                <td>
                                    @if (ins.Status == "Approved")
                                    {
                                        <span class="badge rounded-pill bg-success-subtle text-success border border-success-subtle px-2">Approved</span>
                                    }
                                    else if (ins.Status == "Pending")
                                    {
                                        <span class="badge rounded-pill bg-warning-subtle text-warning-emphasis border border-warning-subtle px-2">Pending</span>
                                    }
                                    else if (ins.Status == "Rejected")
                                    {
                                        <span class="badge rounded-pill bg-danger-subtle text-danger border border-danger-subtle px-2">Rejected</span>
                                    }
                                    else
                                    {
                                        <span class="badge rounded-pill bg-secondary-subtle text-secondary border border-secondary-subtle px-2">Not Applied</span>
                                    }
                                </td>
                                <td class="pe-3">
                                    <div class="d-flex gap-1">
                                        <a href="#" class="btn btn-sm btn-outline-secondary py-0 px-2" style="font-size: 12px;">Profile</a>
                                        @if (ins.Status == "Pending")
                                        {
                                            <a asp-action="Pending_Approvals" class="btn btn-sm btn-success py-0 px-2" style="font-size: 12px;">Review</a>
                                        }
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>

        <div class="card-footer bg-white d-flex justify-content-between align-items-center">
            <small class="text-muted">Showing 1–@Model.Count() of @Model.Count() instructors</small>
            <nav>
                <ul class="pagination pagination-sm mb-0">
                    <li class="page-item disabled"><a class="page-link" href="#">Previous</a></li>
                    <li class="page-item active"><a class="page-link" href="#">1</a></li>
                    <li class="page-item disabled"><a class="page-link" href="#">Next</a></li>
                </ul>
            </nav>
        </div>

    </div>

</main>

<script>
    const searchInput = document.getElementById('instructorSearch');
    const statusFilter = document.getElementById('statusFilter');
    const rows = document.querySelectorAll('#instructorTableBody tr');

    function filterTable() {
        const searchTerm = searchInput.value.toLowerCase();
        const status = statusFilter.value;

        rows.forEach(row => {
            const name = row.getAttribute('data-name');
            const rowStatus = row.getAttribute('data-status');
            
            const matchesSearch = name.includes(searchTerm);
            const matchesStatus = status === 'all' || rowStatus === status;

            if (matchesSearch && matchesStatus) {
                row.style.display = '';
            } else {
                row.style.display = 'none';
            }
        });
    }

    searchInput.addEventListener('input', filterTable);
    statusFilter.addEventListener('change', filterTable);
</script>

