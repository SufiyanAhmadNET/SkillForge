@using SkillForge.Areas.Admin.Models
@model IEnumerable<InstructorListVM>
@{
    ViewData["Title"] = "Instructors";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<main class="p-4 flex-grow-1 admin-bg">

    <div class="d-flex align-items-center justify-content-between mb-4">
        <div class="page-title-bar">
            <h5 class="fw-bold mb-0">Instructor List</h5>
            <p class="text-muted mb-0" style="font-size:14px;">All approved instructors on SkillForge.</p>
        </div>
        <a href="#" class="btn btn-sm btn-outline-success">
            <i class="bi bi-download me-1"></i>Export CSV
        </a>
    </div>

    <!-- Filter row -->
    <div class="d-flex align-items-center gap-3 mb-3 flex-wrap">
        <div class="input-group input-group-sm" style="max-width:260px;">
            <span class="input-group-text bg-white"><i class="bi bi-search text-muted"></i></span>
            <input type="text" class="form-control" id="instructorSearch" placeholder="Search by name…">
        </div>
        <select class="form-select form-select-sm" id="statusFilter" style="max-width:150px;">
            <option value="all">All Status</option>
            <option value="Approved">Approved</option>
            <option value="Pending">Pending</option>
            <option value="Rejected">Rejected</option>
            <option value="NotApplied">Not Applied</option>
        </select>
        <small class="text-muted ms-auto">Showing @Model.Count() instructors</small>
    </div>

    <!-- INSTRUCTORS TABLE -->
    <div class="card border-0 admin-card">
        <div class="card-body p-0">
            <table class="table table-hover mb-0">
                <thead class="table-light">
                    <tr>
                        <th>#</th>
                        <th>Instructor</th>
                        <th>Email</th>
                        <th>Courses</th>
                        <th>Total Students</th>
                        <th>Joined</th>
                        <th>Status</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody id="instructorTableBody">
                    @{
                        int index = 1;
                    }
                    @foreach (var ins in Model)
                    {
                        <tr data-status="@ins.Status" data-name="@ins.Name.ToLower()">
                            <td class="text-muted">@index++</td>
                            <td>
                                <div class="d-flex align-items-center gap-2">
                                    <div class="tbl-avatar bg-primary bg-opacity-10 text-primary">
                                        @ins.Name.Substring(0, 1).ToUpper()
                                    </div>
                                    @ins.Name
                                </div>
                            </td>
                            <td class="text-muted">@ins.Email</td>
                            <td>@ins.TotalCourses</td>
                            <td>@ins.TotalStudents.ToString("N0")</td>
                            <td class="text-muted small">@ins.JoinedDate.ToString("MMM dd, yyyy")</td>
                            <td>
                                @if (ins.Status == "Approved")
                                {
                                    <span class="badge bg-success">Approved</span>
                                }
                                else if (ins.Status == "Pending")
                                {
                                    <span class="badge bg-warning text-dark">Pending</span>
                                }
                                else if (ins.Status == "Rejected")
                                {
                                    <span class="badge bg-danger">Rejected</span>
                                }
                                else
                                {
                                    <span class="badge bg-secondary">Not Applied</span>
                                }
                            </td>
                            <td>
                                <div class="d-flex gap-2">
                                    <a href="#" class="btn btn-sm btn-outline-secondary">Profile</a>
                                    @if (ins.Status == "Pending")
                                    {
                                        <a asp-action="Pending_Approvals" class="btn btn-sm btn-success">Review</a>
                                    }
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>

        <div class="card-footer bg-white d-flex justify-content-between align-items-center">
            <small class="text-muted">Showing 1–@Model.Count() of @Model.Count() instructors</small>
            <nav>
                <ul class="pagination pagination-sm mb-0">
                    <li class="page-item disabled"><a class="page-link" href="#">Previous</a></li>
                    <li class="page-item active"><a class="page-link" href="#">1</a></li>
                    <li class="page-item disabled"><a class="page-link" href="#">Next</a></li>
                </ul>
            </nav>
        </div>

    </div>

</main>

<script>
    const searchInput = document.getElementById('instructorSearch');
    const statusFilter = document.getElementById('statusFilter');
    const rows = document.querySelectorAll('#instructorTableBody tr');

    function filterTable() {
        const searchTerm = searchInput.value.toLowerCase();
        const status = statusFilter.value;

        rows.forEach(row => {
            const name = row.getAttribute('data-name');
            const rowStatus = row.getAttribute('data-status');
            
            const matchesSearch = name.includes(searchTerm);
            const matchesStatus = status === 'all' || rowStatus === status;

            if (matchesSearch && matchesStatus) {
                row.style.display = '';
            } else {
                row.style.display = 'none';
            }
        });
    }

    searchInput.addEventListener('input', filterTable);
    statusFilter.addEventListener('change', filterTable);
</script>

